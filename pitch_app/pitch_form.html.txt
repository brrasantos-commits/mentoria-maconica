{% extends 'base.html' %}

{% block title %}Pitch | Sales Pitch AI{% endblock %}

{% block content %}
<section>
  <div>
    <h2>Etapa 2 — Envio do vídeo</h2>
    <p>Depois de estudar os materiais, envie o vídeo do vendedor para iniciar a análise automática.</p>
  </div>

  <form method="post" action="/analyze" enctype="multipart/form-data" class="material-card" style="gap: 18px;">
    <div class="form-row">
      <div>
        <label for="seller_name">Nome do vendedor</label>
        <input
          type="text"
          id="seller_name"
          name="seller_name"
          placeholder="Ex.: Maria Silva"
          required
        >
      </div>
    </div>

    <div class="form-row">
      <div>
        <label for="video">Vídeo do pitch</label>
        <input
          type="file"
          id="video"
          name="video"
          accept="video/*"
          required
        >
      </div>
    </div>

    <div class="card-note">
      <strong>Materiais de apoio</strong>
      <p style="margin-top: 8px;">
        {% if materials %}
          Você selecionou {{ materials|length }} material(is) para a análise.
        {% else %}
          Nenhum material foi selecionado na etapa de estudo.
        {% endif %}
      </p>
    </div>

    {% if materials %}
    <div class="material-card" style="padding: 14px; box-shadow: none;">
      <h3 style="margin-bottom: 10px;">Materiais selecionados</h3>
      <ul style="margin: 0; padding-left: 18px; color: var(--muted);">
        {% for material in materials %}
          <li>{{ material.title }} — {{ material.industry }} / {{ material.solution }}</li>
        {% endfor %}
      </ul>
    </div>
    {% endif %}

    <button type="submit" class="primary-btn">Enviar para análise</button>
  </form>
</section>
{% endblock %}


--------------------------------

{% extends 'base.html' %}

{% block title %}Pitch | Sales Pitch AI{% endblock %}

{% block content %}
<section>
  <div>
    <h2>Etapa 2 — Envio do vídeo</h2>
    <p>Depois de estudar, envie o vídeo do vendedor para iniciar a análise.</p>
  </div>

  <form method="post" action="/analyze" enctype="multipart/form-data" class="material-card" style="gap:16px;">
    <div>
      <label for="seller_name">Nome do vendedor</label>
      <input type="text" id="seller_name" name="seller_name" required>
    </div>

    <div>
      <label for="video">Vídeo do pitch</label>
      <input type="file" id="video" name="video" accept="video/*" required>
    </div>

    <div>
      <label for="materials">Materiais de apoio</label>
      <select id="materials" name="materials" multiple size="8" required>
        {% for material in materials %}
          <option value="{{ material.filename }}">{{ material.title }} — {{ material.filename }}</option>
        {% endfor %}
      </select>
    </div>

    <div class="card-note">
      Selecione um ou mais materiais de apoio para a análise do pitch.
    </div>

    <button type="submit" class="primary-btn">Enviar para análise</button>
  </form>
</section>
{% endblock %}


-----------------------------------------------


{% extends 'base.html' %}

{% block title %}Pitch | Sales Pitch AI{% endblock %}

{% block content %}
<section>
  <div>
    <h2>Etapa 2 — Envio do vídeo</h2>
    <p>Depois de estudar, envie o vídeo do vendedor para iniciar a análise.</p>
  </div>

  <form method="post" action="/analyze" enctype="multipart/form-data" class="material-card" style="gap:16px;">
    <div>
      <label for="seller_name">Nome do vendedor</label>
      <input type="text" id="seller_name" name="seller_name" required>
    </div>

    <div>
      <label for="video">Vídeo do pitch</label>
      <input type="file" id="video" name="video" accept="video/*" required>
    </div>

    <div>
      <label for="materials">Materiais de apoio</label>
      <select id="materials" name="materials" multiple size="8" required>
        {% for material in materials %}
          <option value="{{ material.filename }}">{{ material.title }} — {{ material.filename }}</option>
        {% endfor %}
      </select>
    </div>

    <div class="card-note">
      Selecione um ou mais materiais de apoio para a análise do pitch.
    </div>

    <button type="submit" class="primary-btn">Enviar para análise</button>
  </form>
</section>
{% endblock %}


------------------------------------

{% extends 'base.html' %}

{% block title %}Pitch | Sales Pitch AI{% endblock %}

{% block content %}
<section>
  <div>
    <h2>Etapa 2 — Envio do vídeo</h2>
    <p>Depois de estudar, envie o vídeo do vendedor para iniciar a análise.</p>
  </div>

  <form method="post" action="/analyze" enctype="multipart/form-data" class="material-card" style="gap:16px;">
    <div>
      abel for="seller_name">Nome do vendedor</label>
      <input type="text" id="seller_name" name="seller_name" required>
    </div>

    <div>
      abel for="video">Vídeo do pitch</label>
      <input type="file" id="video" name="video" accept="video/*" required>
    </div>

    <div>
      abel for="materials">Materiais de apoio</label>
      <select id="materials" name="materials" multiple size="8" required>
        {% for material in materials %}
          <option value="{{ material.filename }}">{{ material.title }} — {{ material.filename }}</option>
        {% endfor %}
      </select>
    </div>

    <div class="card-note">
      Selecione um ou mais materiais de apoio para a análise do pitch.
    </div>

    <button type="submit" class="primary-btn">Enviar para análise</button>
  </form>
</section>
{% endblock %}


----------------------------------

{% extends 'base.html' %}

{% block title %}Pitch | Sales Pitch AI{% endblock %}

{% block content %}
<section>
  <div>
    <h2>Etapa 2 — Envio do vídeo</h2>
    <p>Depois de estudar, envie o vídeo do vendedor para iniciar a análise.</p>
  </div>

  <form method="post" action="/analyze" enctype="multipart/form-data" class="material-card" style="gap:16px;">
    <div>
      <label for="seller_name">Nome do vendedor</label>
      <input type="text" id="seller_name" name="seller_name" required>
    </div>

    <div>
      <label for="video">Vídeo do pitch</label>
      <input type="file" id="video" name="video" accept="video/*" required>
    </div>

    <div>
      <label for="materials">Materiais de apoio</label>
      <select id="materials" name="materials" multiple size="8" required>
        {% for material in materials %}
          <option value="{{ material.filename }}">{{ material.title }} — {{ material.filename }}</option>
        {% endfor %}
      </select>
    </div>

    <div class="card-note">
      Selecione um ou mais materiais de apoio para a análise do pitch.
    </div>

    <button type="submit" class="primary-btn">Enviar para análise</button>
  </form>
</section>
{% endblock %}


------------------------------------

{% extends 'base.html' %}

{% block title %}Pitch | Sales Pitch AI{% endblock %}

{% block content %}
<section>
  <div>
    <h2>Etapa 2 — Envio do vídeo</h2>
    <p>Depois de estudar, envie o vídeo do vendedor para iniciar a análise.</p>
  </div>

  <form method="post" action="/analyze" enctype="multipart/form-data" class="material-card" style="gap:16px;">
    <div>
      <label for="seller_name">Nome do vendedor</label>
      <input type="text" id="seller_name" name="seller_name" required>
    </div>

    <div>
      <label for="video">Vídeo do pitch</label>
      <input type="file" id="video" name="video" accept="video/*" required>
    </div>

    <div>
      <label for="materials">Materiais de apoio</label>
      <select id="materials" name="materials" multiple size="8" required>
        {% for material in materials %}
          <option value="{{ material.filename }}">{{ material.title }} — {{ material.filename }}</option>
        {% endfor %}
      </select>
    </div>

    <div class="card-note">
      Selecione um ou mais materiais de apoio para a análise do pitch.
    </div>

    <button type="submit" class="primary-btn">Enviar para análise</button>
  </form>
</section>
{% endblock %}


---------------------------------------------

