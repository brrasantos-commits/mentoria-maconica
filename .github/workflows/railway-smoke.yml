**Integrating git commands**

I need to make sure we add the suggested steps, which include using `git status` and `git log -1 --oneline`. These probably fit in the run section and should follow the checkout step in the workflow. I'll keep the original code intact and just insert the commands where they need to go. The final output will just be the modified YAML code with that additional step included. Alright, I'll get to crafting it!**Integrating git commands**

I need to make sure we add the suggested steps, which include using `git status` and `git log -1 --oneline`. These probably fit in the run section and should follow the checkout step in the workflow. I'll keep the original code intact and just insert the commands where they need to go. The final output will just be the modified YAML code with that additional step included. Alright, I'll get to crafting it!```yaml
name: railway-smoke
on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Git status and last commit
        shell: bash
        run: |
          git status
          git log -1 --oneline

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install pytest httpx pytest-playwright
          python -m playwright install --with-deps chromium

      - name: Wait for Railway health
        shell: bash
        env:
          BASE_URL: ${{ secrets.RAILWAY_BASE_URL }}
        run: |
          if [ -z "$BASE_URL" ]; then
            echo "RAILWAY_BASE_URL secret is not set";
            exit 1;
          fi

          BASE_URL=${BASE_URL%/}

          for i in {1..30}; do
            if curl -fsS "$BASE_URL/health" > /dev/null; then
              echo "Railway app is healthy"
              exit 0
            fi
            sleep 5
          done

          echo "Railway app did not become healthy in time"
          curl -v "$BASE_URL/health" || true
          exit 1

      - name: Run API smoke tests against Railway
        shell: bash
        env:
          BASE_URL: ${{ secrets.RAILWAY_BASE_URL }}
          E2E_ADMIN_USER: ${{ secrets.E2E_ADMIN_USER }}
          E2E_ADMIN_PASSWORD: ${{ secrets.E2E_ADMIN_PASSWORD }}
        run: |
          pytest -q tests/api

      - name: Run UI E2E smoke tests against Railway
        shell: bash
        env:
          BASE_URL: ${{ secrets.RAILWAY_BASE_URL }}
          E2E_ADMIN_USER: ${{ secrets.E2E_ADMIN_USER }}
          E2E_ADMIN_PASSWORD: ${{ secrets.E2E_ADMIN_PASSWORD }}
        run: |
          pytest -q tests/e2e --browser chromium --tracing retain-on-failure --screenshot only-on-failure

      - name: Upload Playwright artifacts (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: |
            test-results/
            playwright-report/
          if-no-files-found: ignore

